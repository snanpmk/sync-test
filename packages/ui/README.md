# 🎨 SynConnect UI System

The core design system and shared component library for all SynConnect applications.

## 📦 What's Inside?
This package contains reusable, highly-styled UI components that ensure visual consistency across the Web, Dashboard, and Admin apps.

- **Design Tokens**: Standardized colors (Electric Green), spacing, and rounding (32px).
- **Branding**: SVG assets and Logo components.
- **Layouts**: Shared containers and section components.
- **Motion**: Standardized `framer-motion` transition presets.

## 🔧 Usage
Add it as a dependency in any app's `package.json`:
```json
{
  "dependencies": {
    "@synconnect/ui": "*"
  }
}
```
